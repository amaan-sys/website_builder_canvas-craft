// Image section component removed. The Image section has been deprecated and removed from the "Add Section" templates.
// Legacy pages that included an 'image' section will still render using a fallback in `SectionRenderer` so existing content remains visible.

export default null;
